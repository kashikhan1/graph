function exportToCsv(id, data, data1, data2, data3) {
    // $.get(`http://localhost:3000/${id}`, function(response) {
    console.log(data)
        // var csv = 'First,Second\n';
        // response.A.forEach(function(row,index) {
        //     csv += row.join(',');
        //     csv += "\n";
        // });
        // var hiddenElement = document.createElement('a');
        // hiddenElement.href = 'data:text/csv;charset=utf-8,' + encodeURI(csv);
        // hiddenElement.target = '_blank';
        // hiddenElement.download = `${id.split('.')[0]}.csv`;
        // hiddenElement.click();
        // })
}